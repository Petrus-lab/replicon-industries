<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Replicon Local Interface</title>
  <style>
    body {
      background-color: #111;
      color: #eee;
      font-family: monospace;
      padding: 20px;
    }
    h1 {
      color: #00ffff;
    }
    .btn {
      background-color: #2fc5a9;
      color: black;
      border: none;
      padding: 8px 16px;
      margin: 5px;
      cursor: pointer;
      font-weight: bold;
    }
    textarea {
      width: 100%;
      height: 120px;
      background-color: #222;
      color: #eee;
      border: 1px solid #444;
      padding: 10px;
    }
    select {
      width: 100%;
      padding: 8px;
      background-color: #222;
      color: #eee;
      border: 1px solid #444;
    }
    .output {
      background-color: #222;
      border: 1px solid #444;
      padding: 10px;
      white-space: pre-wrap;
      margin-top: 20px;
      min-height: 80px;
    }
  </style>
</head>
<body>
  <h1>🧠 Replicon Local Runner Interface</h1>

  <button class="btn" onclick="sendQuick('vercel-deploy')">Deploy</button>
  <button class="btn" onclick="sendQuick('git-push')">Git Push</button>
  <button class="btn" onclick="sendQuick('firestore-test')">Firestore Test</button>
  <button class="btn" onclick="sendQuick('git-pull')">Git Pull</button>

  <h3>📌 Action Type:</h3>
  <select id="actionType">
    <option value="run-shell">Run Shell Command</option>
    <option value="run-python">Run Python</option>
    <option value="read-file">Read File</option>
    <option value="write-file">Write File</option>
  </select>

  <h3>📄 Paste your instructions below:</h3>
  <textarea id="input" placeholder="Paste commands, files, or instructions here..."></textarea>
  <br />
  <button onclick="submitToRunner()" class="btn">Submit to Runner</button>

  <div class="output" id="output">Awaiting input...</div>

  <script>
    async function sendQuick(action) {
      try {
        const res = await fetch('http://localhost:3001/run', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ action })
        });
        const data = await res.text();
        document.getElementById('output').innerText = data;
      } catch (err) {
        document.getElementById('output').innerText = '❌ Runner not responding';
      }
    }

    async function submitToRunner() {
      const actionType = document.getElementById('actionType').value;
      const input = document.getElementById('input').value;

      const responseDisplay = document.getElementById('output');
      responseDisplay.innerText = '⏳ Processing...';

      try {
        const res = await fetch('http://localhost:3001/run', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            action: actionType,
            command: input
          })
        });

        const data = await res.json();
        responseDisplay.innerText = data.output || '✅ Command executed with no output.';
      } catch (err) {
        responseDisplay.innerText = '❌ Communication failure with runner.';
      }
    }
  </script>
</body>
</html>
